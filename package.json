{
  "name": "spectrum-router-subgraph",
  "private": true,
  "license": "MIT",
  "version": "1.0.0",
  "repository": "https://github.com/Spectrum-Digital/spectrum-router-subgraph.git",
  "scripts": {
    "codegen": "graph codegen src/subgraph.yaml --output-dir src/generated/",
    "build": "graph build src/subgraph.yaml",
    "auth": "graph auth --product hosted-service",
    "prepare:arbitrum": "mustache src/config/arbitrum.json src/template-arbitrum.yaml > src/subgraph.yaml",
    "prepare:base": "mustache src/config/base.json src/template-base.yaml > src/subgraph.yaml",
    "prepare:fantom": "mustache src/config/fantom.json src/template-fantom.yaml > src/subgraph.yaml",
    "deploy:arbitrum": "pnpm prepare:arbitrum && graph deploy --product hosted-service coinlordd/spectrum-router-arbitrum src/subgraph.yaml",
    "deploy:base": "pnpm prepare:base && graph deploy --studio spectrum-router-base src/subgraph.yaml",
    "deploy:fantom": "pnpm prepare:fantom && graph deploy --product hosted-service coinlordd/spectrum-router-fantom src/subgraph.yaml"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.61.0",
    "@graphprotocol/graph-ts": "^0.31.0",
    "mustache": "^4.2.0",
    "typescript": "^5.2.2"
  }
}
